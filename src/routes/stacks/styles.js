import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    padding: {paddingVertical: 10},
    button: {fontWeight: '600', fontSize: 18, paddingLeft: 10, color: '#fd7e14'}
})